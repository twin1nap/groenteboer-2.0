what to keep from Original project, forms have no sql code exept form1, dont need to touch anything but form1
from1 just has get.

classlibrary:
passtrough string connectionString

folder models for classes, class data for sql code.

Original project had older mysql dll, because it gets compiled after the classlibrary(with the same dll but newer) it replaces the old file, which then overwrites the dll of the newer version causing an error, remove the dll because its not needed anymore.

todo:
 should i add an archive filter(only non active products)
categories adaptive in store?(tab pages generated.)
updtabale price type.
unactive products visably unactive before editing them.

planning:
	dag 14-01-2026:	product management(currently called product manager) full crud & new standard.
	dag 15-01-2026	groenteboer app transaction saved in database.
				product management UI(categorized) en raports.
	dag 19-01-2026		finish raports.
				product managemet graph

new standard - done 12:32 14-01-2026, also added image update support
catagory edit - done 16:00 14-01-2026
add done - 16:20 14-01-2026
added soft delete as checkbox for active products - 9:45 1501-2026
full crud done - 10:38 15-01-2026
